

class Blockchain{
  final Blockchain? blockChain;
  final String? url;
  final String? socket5;
  final String? retry;
  final String? timeOut;

  Blockchain({this.blockChain, this.url, this.socket5, this.retry, this.timeOut});
}